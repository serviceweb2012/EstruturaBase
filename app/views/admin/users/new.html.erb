<% content_for :sidebar do %>
  <%= error_message_for(@user) %>
<% end %>

<% content_for :main do  %>
  <%= onde_estou "Cadastro de #{@user.class.human_name}",admin_root_path,'Dashboard' %>
  <div class="box fields">
    <%= form_for [:admin,@user] do |f| %>
      <%= render :partial => 'form',:locals =>{:button_label => "Cadastrar",:f => f} %>
    <% end %>
  </div>
<% end %>

